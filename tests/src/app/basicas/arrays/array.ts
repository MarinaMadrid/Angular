export function obtenerRobots() {
  //return ['MegaMan', 'Jarvis', 'Robocop'];
  return ['MegaMan', 'Jarvis', 'Robocop', 'Ultron']; //si se borra Ultron, daría error en el último test
  //si se añade uno más, daría error en el primer test, al ser más de 4
}
